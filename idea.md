{
task : Build a cli based freedium app using the web repo. Take files and context necessary to build the cli tool. After building it i will remove it from the fredium-cli folder; 
context : A cli app called medium-cli already exists but there is no freedium alternative. For hackers cli is the default user interface and medium is the defaut google news. So combining this two is the best option;
example : freedium <url>;
publishing : My plan is to put in npm. So anyone can install it using npm install -g freediumcli;
rules : Dont build a web app.;
}
